<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sale Products</title>
</head>
<style>
    /* เริ่มตั้งค่าพื้นฐาน */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4; /* กำหนดสีพื้นหลัง */
}

/* ส่วนของ .sale-products */
.sale-products {
    padding: 20px 10%;
    background-color: #ffffff; /* พื้นหลังของหน้า sale */
    text-align: center;
}

.product-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    gap: 20px;
    margin-top: 20px;
}

/* ส่วนของแต่ละสินค้า */
.product {
    background-color: #ffffff;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    padding: 15px;
    width: 280px;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* เอฟเฟ็กต์เมื่อ hover */
.product:hover {
    transform: scale(1.05); /* ขยายขนาดเล็กน้อย */
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.2); /* เพิ่มเงา */
}

.product img {
    width: 100%;
    height: auto;
    border-radius: 8px;
    object-fit: cover;
    margin-bottom: 10px;
}

.product p {
    font-size: 16px;
    color: #333;
}

.product p strong {
    font-weight: bold;
}

.product p del {
    color: red; /* สีของราคาต้นฉบับ */
    font-size: 14px;
    margin-left: 10px;
}

.product p span {
    font-weight: bold;
    color: #2ECC71; /* สีเขียวสำหรับราคาลด */
    font-size: 18px;
}

/* Responsive Design สำหรับมือถือ */
@media (max-width: 768px) {
    .sale-products {
        padding: 20px 5%;
    }

    .product-container {
        justify-content: center;
    }

    .product {
        width: 100%; /* ทำให้แต่ละสินค้าเต็มพื้นที่ในหน้าจอ */
        max-width: 300px;
    }
}

</style>
<body>

    <%- include("partials/header"); -%>

    <section class="sale-products">
        <div class="product-container">
            <% discountedProducts.forEach((product) => { %>
                <div class="product" data-id="<%= product.id %>">
                    <img src="/image/<%= product.image && product.image[0] ? product.image[0].name : 'placeholder.jpg' %>" alt="Product Image">
                    <p><%= product.name %></p>
                    <p><strong>Sale Price:</strong> $<%= product.salePrice %> <del>$<%= product.originalPrice %></del></p>
                </div>
            <% }); %>
        </div>
    </section>

</body>
</html>
